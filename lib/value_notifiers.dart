import 'package:flutter/material.dart';

Color trackColor = Colors.green;
Color alarmColor = Colors.green;

ValueNotifier<Color> doorColor = ValueNotifier(Colors.green);
//ValueNotifier<Color> redColor = ValueNotifier(Colors.red);
